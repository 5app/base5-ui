import {Meta, Story, Canvas, ArgsTable} from '@storybook/addon-docs/blocks';

import ViewMoreText from './';

<Meta title="Components/ViewMoreText" component={ViewMoreText} />

# ViewMoreText

```jsx
import ViewMoreText from 'base5-ui/ViewMoreText';
```

Truncates a piece of text at a specified length, and displays a "View more" button which reveals the rest of the text when clicked. When the text is only slightly shorter than the specified length, so that revealing it would take up just as much space as the "View more" button did, the text is not truncated. You can tweak this behaviour with the `tolerance` prop.

The labels of the "View more" and "View less" buttons can be customised using the props `viewMoreLabel` and `viewLessLabel`.

## Examples

<Canvas>
	<Story
		name="Simple example"
		args={{
			content:
				'This is a short example text with a disappointing reveal.',
			maxLength: 26,
		}}
	>
		{props => <ViewMoreText {...props} />}
	</Story>
</Canvas>

<Canvas>
	<Story
		name="Tolerance example"
		args={{
			content:
				'This text is longer than the specified limit, but only by a small amount, so it would be pointless to truncate it.',
			maxLength: 85,
		}}
	>
		{props => <ViewMoreText {...props} />}
	</Story>
</Canvas>

<Canvas>
	<Story
		name="Custom labels"
		args={{
			content:
				'Here comes a longer, tastier example with custom labels for the "View more"/"View less" buttons. Beetroot water spinach okra water chestnut ricebean pea catsear courgette summer purslane. Water spinach arugula pea tatsoi aubergine spring onion bush tomato kale radicchio turnip chicory salsify pea sprouts fava bean. Dandelion zucchini burdock yarrow chickpea dandelion sorrel courgette turnip greens tigernut soybean radish artichoke wattle seed endive groundnut broccoli arugula.',
			maxLength: 110,
			viewMoreLabel: 'Read more',
			viewLessLabel: "I don't care about any of that",
		}}
	>
		{props => <ViewMoreText {...props} />}
	</Story>
</Canvas>

## Props

<ArgsTable story="Simple example" />
