import {Meta, Story, Canvas, ArgsTable} from '@storybook/addon-docs/blocks';
import LinkTo from '@storybook/addon-links/react';

import Card from '../Card';
import Stack from '../Stack';

import Pill from './';

<Meta title="Components/Pill" component={Pill} />

# Pill

```jsx
import Pill from 'base5-ui/Pill';
```

The Pill component is great for representing simple items in the UI: assets, tags, categories, teams, or users.

Pills can have an icon and an optional "Delete" button which should remove the associated item from the list it's in.

Pills can be made clickable, but should never be used as action buttons (think "Confirm" or "Cancel" buttons). Use the <LinkTo kind="Components/Button">Button</LinkTo> component for this use case.

## Non-interactive examples

<Canvas>
	<Story name="Non-interactive">
		<Stack spacing="xs">
			<Pill>Regular pill</Pill>
			<Pill dimmed>Dimmed pill</Pill>
			<Pill background="highlight">
				Pill with custom background colour
			</Pill>
			<Pill icon="tag" iconColor="neutral">
				Pill with icon
			</Pill>
			<Pill dimmed icon="user" iconColor="negative">
				Dimmed pill with icon
			</Pill>
		</Stack>
	</Story>
</Canvas>

## Interactive examples

To make a Pill's body clickable, simply attach an `onClick` handler. You can also make the Pill a link: Use the `as` prop to choose which element to render, e.g. `"a"` or react-router's `{Link}`. You can then use the `href` or `to` prop to define the link's destination. All other link props will also be passed through to the clickable element.

<Canvas>
	<Story name="Interactive">
		<Stack spacing="xs">
			<Pill onDelete={() => console.log('Delete')} deleteLabel="Delete">
				Regular, delete-able pill
			</Pill>
			<Pill
				dimmed
				onDelete={() => console.log('Delete')}
				deleteLabel="Delete"
			>
				Dimmed, delete-able pill
			</Pill>
			<Pill
				icon="tag"
				iconColor="neutral"
				onDelete={() => console.log('Delete')}
				deleteLabel="Delete"
			>
				Delete-able Pill with icon
			</Pill>
			<Pill onClick={() => console.log('Hello!')}>Clickable pill</Pill>
			<Pill
				onClick={() => console.log('Hello!')}
				icon="external"
				iconColor="pink"
			>
				Clickable pill with icon
			</Pill>
			<Pill
				onClick={() => console.log('Hello!')}
				icon="user"
				iconColor="positive"
				onDelete={() => console.log('Delete')}
				deleteLabel="Delete"
			>
				Clickable, delete-able pill
			</Pill>
			<Card maxWidth={250} variant="inverted" p="s">
				<Stack spacing="xs">
					<Pill
						onClick={() => console.log('Hello!')}
						icon="user"
						iconColor="positive"
					>
						Pill on a dark background
					</Pill>
					<Pill
						onClick={() => console.log('Hello!')}
						icon="user"
						iconColor="positive"
						onDelete={() => console.log('Delete')}
						deleteLabel="Delete"
					>
						Pill with a long label that doesn't fit the parent
						container
					</Pill>
				</Stack>
			</Card>
		</Stack>
	</Story>
</Canvas>

## Playground

<Canvas>
	<Story
		name="Playground"
		args={{
			children: "A pill that won't make you feel sick",
			deleteLabel: 'Delete',
			icon: 'ok',
			iconColor: 'links',
			onDelete: () => console.log('Delete!'),
			onClick: null,
		}}
	>
		{props => <Pill {...props} />}
	</Story>
</Canvas>

## Props

In addition to the props below, the `Pill` component also supports
the margin style props (e.g. `m`, `ml`, `mx` etc).

<ArgsTable story="Playground" />
