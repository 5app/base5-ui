---
name: Flex
menu: Components
---

import {Playground, Props} from 'docz';
import Flex, {Box} from './';

# Flex

`Flex` is a container component for building layouts using CSS flexbox. It's especially useful when used with the `Box` component which provides control for laying out the children.

The component is built on top of `Box` and its props `align`, `column`, and `wrap` are aliases for the `Box` props `alignItems`, `stackItems`, and `wrapItems`.

## Examples

<Playground>
	<Flex align="top">
		<Box grow shrink>
			I take up all the free space and push other Boxes away! 👉
		</Box>
		<Box>👀 I'm smol.</Box>
	</Flex>

	<Flex wrap align="top" mt="m">
		<Box grow shrink>
			Still taking up all free space.
		</Box>
		<Box>If there's no room for me I'll wrap onto a new line 👇</Box>
	</Flex>
</Playground>

## Props

<Props of={Flex} />
