---
name: Base
menu: Components
---

import { Playground } from 'docz'
import Base from './'

# Base

Provides a flexible 'as' prop for passing a component or element to render.
Use 'refKey' to specify custom ref props (i.e. 'innerRef')

<Playground>
	<Base as="span">Inline</Base>
	<Base as="div">Block</Base>
</Playground>